export type TimeInputModel = {
    startTime: string,
    endTime: string,
    weekDay: string,
    description: string
}
export type TimeUpdateInputModel = {
    startTime: string,
    endTime: string,
    weekDay: string
};
